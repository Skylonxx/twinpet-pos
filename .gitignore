node_modules/
.DS_Store
.env
.env.local
npm-debug.log
yarn-error.log
dist/
build/
.vscode/
.idea/
# Generated from firebase.json by scripts/gen-deploy-config.mjs (run on dev/build/deploy)
functions/src/deployConfig.ts
# Local Firebase emulator data dump (seed + test state persisted between runs)
.emulator-data/
firebase-debug.log
firestore-debug.log
ui-debug.log
# Local emulator data (dev:emulator script)
emulator-data/
# Custom pos-db snapshot (named-db persistence; kept OUTSIDE emulator-data so
# firebase --export-on-exit can't wipe it). See scripts/pos-db-snapshot.mjs.
.local-persist/
# Firebase emulator export dumps
firebase-export-*/
# Playwright output
playwright-report/
test-results/
# Cursor: ignore local workspace settings, but KEEP shared project rules
/.cursor/*
!/.cursor/rules/
# Claude Code local tooling config — never commit
.claude/
